# タスクリスト: ゴールシーン動画モーダルの実装

- [x] 実装計画の作成
- [x] `page-dashboard.php` の修正
    - [x] `#token-modal` に「動画を見る」ボタンの構造を追加
    - [x] JavaScript関数 `openGoalScene` を追加
    - [x] `closeTokenModal` を更新してボタンを非表示にする処理を追加
    - [x] 必要に応じて `aspect-square` クラスの挙動をリセット（表示確認後）
    - [x] ゴールシーンのPHPループを `openGoalScene` を使用するように更新
- [x] 変更の検証
    - [x] 通常のNFTモーダルの確認（以前と同様に機能すること）
    - [x] 動画なしのゴールシーンの確認（画像のみ表示されること）
    - [x] 動画ありのゴールシーンの確認（画像＋ボタンが表示されること）
    - [x] ボタンクリックで動画が開くことの確認
- [x] ウォークスルーの作成
